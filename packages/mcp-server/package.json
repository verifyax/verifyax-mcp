{
  "name": "@verifyax/mcp-server",
  "version": "0.1.0",
  "description": "MCP server exposing the VerifyAX agent-evaluation platform as conversational tools.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "verifyax-mcp-server": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "test:conformance": "tsc -b && vitest run --config vitest.conformance.config.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@verifyax/sdk": "workspace:*",
    "zod": "^3.24.1"
  },
  "publishConfig": {
    "access": "public"
  }
}
